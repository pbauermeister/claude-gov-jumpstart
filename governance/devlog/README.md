# Governance devlogs

This folder holds devlog entries for tasks tagged `[gov]` — governance maintenance, evolution, and bootstrap.
Other task categories (`[arch]`, `[impl]`, `[study]`) place their devlogs under `architecture/devlog/`.

The standard devlog structure and review-gate granularity live in `architecture/devlog/CLAUDE.md`; the same conventions apply here.
